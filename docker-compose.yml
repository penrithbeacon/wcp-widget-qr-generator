services:
  qr-generator:
    build: .
    image: wcp-widget-qr-generator
    container_name: wcp-widget-qr-generator
    ports:
      - "3738:3738"
    volumes:
      - ./published:/app/published
    restart: unless-stopped
